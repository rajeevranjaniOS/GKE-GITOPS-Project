apiVersion: v1
kind: Service

metadata:
  name: shippingservice
  namespace: online-boutique

spec:
  selector:
    app: shippingservice

  ports:
  - port: 50051
    targetPort: 50051

  type: ClusterIP